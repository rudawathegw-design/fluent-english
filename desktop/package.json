{
  "name": "fluent-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Fluent — desktop build (Electron). Wraps the same www/ app as the Android version.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder --win nsis --publish never"
  },
  "dependencies": {
    "electron-updater": "^6.3.9"
  },
  "devDependencies": {
    "electron": "^31.7.7",
    "electron-builder": "^24.13.3"
  }
}
